name: Secret Scan

on:
  push:

  pull_request:

jobs:
  gitleaks:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Scan Repository

        uses: gitleaks/gitleaks-action@v2
